# Bolt is rewritten in Rust

Bolt is implemented in Rust. The requirements and test suite are
language-neutral and carry the behaviour into the rewrite. The legacy Go
implementation remains a reference for behaviour, not a source of requirements.

The derivation record establishes provenance. The implementation language does
not.
